

export const domainURL = 'https://www.minet.com/';
export const params = window.location.search.substr(1).split(',');
export const localhost = window.location.hostname.indexOf('localhost') !== -1;
export const moderator = window.location.pathname.indexOf("moderator") !== -1 
